<%= simple_form_for(resource, as: resource_name,
                    url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <%= f.input :shop_owner, as: :hidden %>
  <%= f.input :email, required: true,
              class: 'form-control' %>
  <%= f.input :password,  required: true,
              class: 'form-control' %>
  <%= f.input :password_confirmation,  required: true,
              class: 'form-control' %>
  <%= f.simple_fields_for :avatar, resource.build_avatar do |ph| %>
    <%= ph.input :image, as: :file, required: true %>
  <% end %>
  <%= f.simple_fields_for :shop, resource.build_shop do |sh| %>
    <%= sh.input :name, as: :string, required: true %>
  <% end %>
  <div class="row">
    <div class="col-xs-5 col-xs-offset-7">
      <%= f.submit t('.sign_up'),
                   class: 'btn btn-primary btn-block btn-flat',
                   id: 'sign_up_submit_btn' %>
    </div><!-- /.col -->
  </div><!-- /.row -->

<% end %>