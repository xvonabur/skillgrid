<!-- Fixed navbar -->
<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
              data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= products_path %>">➚#</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <% if !!current_user %>
          <li class="dropdown" id="user-dropdown-menu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
               aria-haspopup="true" aria-expanded="false">
              <%= current_user.name %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Выход', destroy_user_session_path,
                                       method: :delete, id: 'user-logout-link' %>
              </li>
            </ul>
          </li>
          <% else %>
            <li class="<%= 'active' if current_page?(new_user_registration_path) %>">
              <%= link_to 'Регистрация', new_user_registration_path, id: 'user-sign-up-link' %>
            </li>
            <li class="<%= 'active' if current_page?(new_user_session_path) %>">
              <%= link_to 'Вход', new_user_session_path, id: 'user-sign-in-link' %>
            </li>
          <% end %>
        </ul>

    </div><!--/.nav-collapse -->
  </div>
</nav>