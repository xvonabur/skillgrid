<% if defined?(type) && defined?(message) %>
  <div id="flash_<%= flash_class(type) %>"
       class="alert <%= flash_class(type) %>">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>